server:
  port: 8083

spring:
  application:
    name: time-client

eureka:
  client:
    service-url:
      #配制eureka的服务端的地址
      defaultZone: http://localhost:8761/eureka
      #无需注册到eureka的服务器中
    register-with-eureka: false
